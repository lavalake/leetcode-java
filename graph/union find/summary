1. link list representation
  Union run time n^2 because we need update every node of the link list for every union. we will have n-1 union operation
2. tree representation
  with rank and compression, run time to O(mlogn)
3. array representation
4. union by rank
5. path compression
